<!DOCTYPE html>
<html class="h-full bg-gray-50 dark:bg-gray-900">
  <head>
    <title>Work&Talent</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="h-full text-gray-900 dark:text-gray-100">
    <div class="min-h-full">
      <%= render "layouts/navbar" %>
      <div class="py-10">
        <header>
          <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
            <h1 class="text-4xl text-center font-bold leading-tight tracking-tight text-gray-900 dark:text-white">
              <%= content_for?(:title) ? yield(:title) : "Bienvenido al futuro con Work&Talent" %>
            </h1>

            <p class="mt-2 text-center text-lg italic text-gray-600 dark:text-gray-400">
              La forma más rápida y sencilla de conectar talento con oportunidades reales
            </p>
          </div>
        </header>
        <main>
          <div class="mx-auto max-w-7xl sm:px-6 lg:px-8">
            <div class="max-w-3xl mx-auto mt-10">
              <div class="mb-6">
                <div class="fixed top-4 left-1/2 transform -translate-x-1/2 z-50 w-full max-w-md">
                  <% flash.each do |type, message| %>
                    <% next if message.blank? %>
                    <div class="mb-4 p-4 rounded-lg shadow-lg text-sm font-medium flex items-center justify-between
                                <%= flash_class_for(type) %>">
                      <span><%= message %></span>
                      <button onclick="this.parentElement.remove()" class="ml-4 text-lg font-bold focus:outline-none">×</button>
                    </div>
                  <% end %>
                </div>

                <%= yield %>
              </div>
            </div>
          </div>
        </main>
      </div>
    </div>
  </body>
</html>
